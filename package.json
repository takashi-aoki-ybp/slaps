{
  "name": "slaps",
  "version": "1.1.0",
  "description": "Nothing but slaps. — HIPHOP Station",
  "main": "script.js",
  "scripts": {
    "build": "node build.js",
    "audit:songs": "node scripts/audit-songs.js",
    "audit:youtube": "node scripts/audit-youtube.js",
    "curate:empty": "node scripts/fill-empty-descriptions.js",
    "android:sync": "npm run build && npx cap sync android",
    "android:verify": "node scripts/verify-android.js",
    "submissions": "node scripts/review-submissions.js",
    "verify": "npm test && npm run build && git diff --check",
    "test": "npm run audit:songs"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "jimp": "^0.16.13"
  }
}
